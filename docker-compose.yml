version: '3'
services:
  streamlitjer:
    container_name: streamlitjer
    image: myfirstimagestreamlitjer
    env_file:
      - .streamlit/secrets.toml
    ports:
      - "8505:8505"
    volumes:
      - /shared/ubuntu/code/streamlitJer:/app
    restart: unless-stopped    